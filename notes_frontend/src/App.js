import React from "react";

// PUBLIC_INTERFACE
export default function App() {
  /** Minimal application shell to verify the preview system can start the React app. */
  return (
    <main style={{ fontFamily: "system-ui, -apple-system, Segoe UI, Roboto, sans-serif", padding: 24 }}>
      <h1 style={{ marginTop: 0 }}>Notes Frontend</h1>
      <p>
        The React dev server is running. This is a minimal scaffold to satisfy the container start command.
      </p>
    </main>
  );
}
